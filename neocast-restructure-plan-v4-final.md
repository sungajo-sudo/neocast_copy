# 네오캐스트 (NeoCast) 전면 재정비 기획안 v4 (최종)

> **작성일:** 2025-02-25  
> **목적:** Claude Code 작업 전, 전체 작업 범위와 전략 확정  
> **상태:** 리뷰 대기 — 확인 후 Claude Code 작업 착수  
> **레포:** https://github.com/sungajo-sudo/neocast_copy  
> **참고 이미지:** 호스트 홈 와이어프레임 (참고용, 알림 등 미정의 요소 무시)

---

## 1. 브랜치 분석 요약

### main vs dev 핵심 차이

| 구분 | main | dev |
|------|------|-----|
| **백엔드** | Fastify + Prisma + PostgreSQL + Redis | Node.js 인메모리 (DB 없음) |
| **인증** | JWT (Access/Refresh) | 닉네임 + 세션코드 |
| **NCode** | wasm-pdf-core + SOBP 할당 | 없음 |
| **메신저** | 친구관리 + DM + 프레즌스 | 없음 |

### 이식 원칙

```
main 브랜치에서 분기한 feature/restructure-v2 에서만 작업
→ main 코드/실서버/실DB 일절 영향 없음
→ dev의 UI/기능을 main 구조 위에 컴포넌트 단위 재작성
→ 나중에 main으로 merge 가능하도록 "삭제" 대신 "제외/비활성화" 방식
```

---

## 2. 제외/비활성화 대상 (main 기능 중)

나중에 main merge 시 문제 없도록 **삭제가 아닌 비활성화** 처리.

| 기능 | 처리 방식 |
|------|----------|
| 메신저 (DM, 친구관리, 프레즌스) | LNB에서 숨김, 라우트 비활성화 (코드 유지) |
| 알림 기능 | 미구현 (정의 없음) |
| 음성 통화 (WebRTC) | 비활성화 |

---

## 3. 로컬 테스트 환경 구축

### Docker Compose + Seed

```bash
docker-compose up -d postgres redis    # 로컬 DB만 사용 (실서버 ❌)
cd Server && npm install
npm run db:generate && npm run db:migrate
npm run db:seed                         # 더미 데이터 + 테스트 계정
npm run dev                             # localhost:8190

cd ../Client/Web && npm install
npm run dev                             # localhost:5173
```

### 테스트 계정 (seed에서 자동 생성)

| 역할 | 이메일 | 비밀번호 |
|------|--------|----------|
| 호스트 | `host@abc.com` | `1234` |
| 게스트 | `guest@abc.com` | `1234` |

- 로그인 페이지에 **"테스트 로그인" 버튼** 추가 (`NODE_ENV=development`일 때만)
- 🎓 호스트 테스트 로그인 / 👤 게스트 테스트 로그인

### 더미 데이터

- 샘플 세션 3~5개 (예정/진행중/종료)
- 각 세션에 더미 스트로크
- 워크시트(PDF) 2~3개
- 수업 결과 데이터

### NCode 처리

- main의 nc-paperhub + wasm-pdf-core 그대로 유지
- 로컬에서 wasm 실패 시 에러 핸들링 ("NCode 변환 불가" 안내)

---

## 4. Phase 1 — 호스트 메뉴 개편

### LNB 구조

```
🏠 홈
📋 세션 목록
📄 내 워크시트
📊 수업 결과
─────────────
👤 프로필 (하단)
```

> ※ main의 메신저 관련 메뉴는 LNB에서 숨김 처리

### 4-1. 홈

**상단 인사 영역:**
- "안녕하세요, {이름}님!" + "오늘도 스마트한 수업을 시작해볼까요?"

**카드 영역 (2컬럼):**

| 좌측 | 우측 |
|------|------|
| 새 세션 시작 카드 (+ 버튼) | 요약 대시보드 |
| "즉시 수업을 시작하거나 교재를 업로드하여 세션을 준비하세요" | 진행 예정: N sessions / 평균 집중도: N% |
| 클릭 → 세션 만들기 모달 | "상세 학습 데이터 리포트 보기 →" 링크 (→ 수업 결과) |

**세션 목록 영역:**
- "세션 목록" 제목 + 우측 "오늘 / 예정된 세션" 필터 탭
- 오늘 기준 최신 세션 **최대 5개** 노출
- 각 세션: 시간, 제목, 강사명, 첨부 워크시트, [시작] 버튼, [...] 더보기

### 4-2. 세션 만들기 모달 (공통)

홈의 "새 세션 시작" 카드, 세션 목록의 상단 버튼에서 동일하게 호출.

| 필드 | 필수 | 설명 |
|------|------|------|
| 수업 제목 | ✅ | 세션 이름 |
| 날짜/시간 | ❌ | 수업 일정 (미입력 시 등록일 기준) |
| 워크시트 업로드 | ❌ | PDF 업로드 → "내 워크시트"에 자동 저장 |
| **참가 예정 인원 수** | ✅ | 숫자 입력 (참여도 산출 기준) |
| 세션 비밀번호 | 자동생성 | 6자리 코드 |
| 게스트 모드 허용 | 체크박스 | 비회원 참가 여부 |
| 세션 생성하기 | 버튼 | 생성 완료 |

> 참가 예정 인원 수: 실제 입장 인원과 비교하여 참여도(%) 산출에 사용

### 4-3. 세션 목록

- 전체 세션 등록일 최신순 노출
- 상단에 세션 만들기 버튼
- 각 세션 카드: 제목, 날짜/시간, 참가자 수(예정/실제), 상태 뱃지, 시작 버튼

### 4-4. 내 워크시트

- 세션 만들기에서 업로드된 PDF 리스트형 노출
- **별도 업로드 없음**
- 다운로드: NCode PDF / .np2

### 4-5. 수업 결과

- **dev Session 2 이식**: ReportDetail, StudentReport 컴포넌트
- **dev Session 7 이식**: PDF 다운로드 (jsPDF + html2canvas)
- 세션별 학생 참여도/성취도 데이터
- AI 리포트 UI
- 홈 대시보드의 "상세 학습 데이터 리포트 보기" 링크 연결

---

## 5. Phase 2 — 호스트 세션 입장 화면

### 5-1. 기본 구조

- main 브랜치 세션 입장 화면 **그대로** (Pages, PDF 업로드, 캔버스 등 이미 있음)
- 별도 이식 불필요 — main에서 분기했으므로 자동 포함

### 5-2. 추가 작업: 상단 뷰 탭

```
[ 기본 뷰 ] [ 참가자 모드 뷰 ]
```

| 탭 | 내용 | 출처 |
|----|------|------|
| 기본 뷰 | main 그대로 | 이미 있음 |
| 참가자 모드 뷰 | 학생 모니터링 그리드 + 첨삭 진입 | dev 이식 |

### 5-3. 참가자 모드 뷰에서 이식할 dev 기능

| 기능 | dev 출처 | 설명 |
|------|----------|------|
| 모니터링 그리드 | Session 8-2 | 접속 학생 캔버스 한눈에 보기 |
| 첨삭 (Red Pen) | dev 핵심 기능 | 학생 캔버스 진입 → 빨간펜 첨삭 |
| **필기중 뱃지** | STEP 4 (신규) | 학생 목록에서 현재 필기중인 학생 표시 |

> ※ STEP 2(Pages), STEP 3(PDF 업로드), STEP 5(그리드뷰 개선)은 main에 이미 있으므로 별도 작업 불필요

---

## 6. Phase 3 — 게스트(학생) 화면

### 6-1. 기본 화면

- main 브랜치 게스트 세션 화면 **그대로** (이미 있음)

### 6-2. 첨삭 수신 모드 (dev 이식)

- 호스트 빨간펜 첨삭 → 학생 캔버스에 실시간 노출
- Socket.IO `/stroke` 네임스페이스 활용

---

## 7. 이식 대상 최종 정리

### dev → feature/restructure-v2 이식 목록

| # | 기능 | dev 출처 | 이식 대상 Phase |
|---|------|----------|----------------|
| 1 | 호스트 홈 세션 목록 UI | Session 1 | Phase 1-1 |
| 2 | 수업 결과 + AI 리포트 | Session 2 | Phase 1-5 |
| 3 | 세션 개설 UI (모달) | Session 6 | Phase 1-2 |
| 4 | 결과 PDF 다운로드 | Session 7 | Phase 1-5 |
| 5 | 모니터링 그리드 뷰 | Session 8-2 | Phase 2-2 |
| 6 | 첨삭 (Red Pen) 기능 | dev 핵심 | Phase 2-2, 3-2 |
| 7 | 필기중 뱃지 | STEP 4 (신규) | Phase 2-3 |

### 이식하지 않는 것

| 항목 | 이유 |
|------|------|
| dev 서버 (demo-server.ts) | main Fastify 서버 사용 |
| dev 인메모리 세션 관리 | main PostgreSQL 사용 |
| dev 수채화/글래스모피즘 스타일 | main 스타일 유지 |
| dev 게스트 캔버스 UI (Session 8-1) | main 게스트 화면 사용 |
| STEP 2, 3, 5 | main에 이미 있음 |

### main에서 비활성화 (삭제 ❌)

| 항목 | 방식 |
|------|------|
| 메신저/DM/친구관리 | LNB 숨김 + 라우트 비활성화 |
| 음성 통화 | 비활성화 |
| 알림 | 미구현 |

---

## 8. 작업 순서 (Claude Code)

```
Phase 0 ── 사전 준비 (30분)
  ├─ main에서 feature/restructure-v2 분기
  ├─ Docker Compose + Prisma seed + 테스트 계정
  ├─ 로그인 테스트 버튼 추가
  └─ 메신저/음성통화 비활성화
       │
       ▼
Phase 1 ── 호스트 메뉴 개편
  ├─ 1-1. LNB 재구성 (홈/세션목록/워크시트/수업결과)
  ├─ 1-2. 홈 화면 (대시보드 + 세션카드 + 세션만들기 모달)
  ├─ 1-3. 세션 목록
  ├─ 1-4. 내 워크시트
  └─ 1-5. 수업 결과 (dev 이식)
       │
       ▼
Phase 2 ── 호스트 세션 화면
  ├─ 2-1. 기본 뷰 / 참가자 모드 뷰 탭 추가
  ├─ 2-2. 참가자 모드 뷰 (모니터링 + 첨삭) dev 이식
  └─ 2-3. 필기중 뱃지 추가
       │
       ▼
Phase 3 ── 게스트 화면
  └─ 3-1. 첨삭 수신 모드 (dev Red Pen 이식)
```

---

## 9. 확정 사항 전체 요약

| 항목 | 결정 |
|------|------|
| 기반 | main에서 분기 (main 영향 ❌) |
| 이식 방식 | 컴포넌트 단위 재작성 (git merge ❌) |
| 스타일 | main 스타일 유지 |
| 용어 | "워크시트"로 통일 |
| 워크시트 별도 업로드 | 제외 |
| 세션 만들기 추가 필드 | **참가 예정 인원 수** |
| 메신저/DM | 비활성화 (삭제 ❌, merge 대비) |
| 음성통화 | 비활성화 |
| 알림 | 미구현 |
| NCode | main 유지 |
| 테스트 계정 | host@abc.com / guest@abc.com |
| STEP 2,3,5 | main에 있으므로 별도 작업 불필요 |
| STEP 4 | 필기중 뱃지만 추가 |
| 참고 이미지 | 와이어프레임 참고용 (알림 등 무시) |

---

*v4 — 전체 피드백 반영 최종본. 확인 후 Claude Code 작업 착수.*
