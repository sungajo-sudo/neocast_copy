/**
 * 세션 목록 페이지
 * Phase 1-3에서 구현 예정:
 * - 전체 세션 등록일 최신순 노출
 * - 상단 "세션 만들기" 버튼
 * - 각 세션 카드: 제목, 날짜/시간, 참가자 수, 상태 뱃지, 시작 버튼
 */

export function SessionListPage() {
  return (
    <div className="p-6">
      <div className="max-w-5xl mx-auto">
        <h1 className="text-2xl font-bold text-gray-900 mb-2">세션 목록</h1>
        <p className="text-gray-500 text-sm">Phase 1-3에서 세션 목록이 여기에 표시됩니다.</p>
      </div>
    </div>
  );
}
