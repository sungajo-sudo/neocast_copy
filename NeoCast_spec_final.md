# NeoCast — 호스트 UI 구현 지침서 (최종본)

> **Version 0.2 | 2026.02.26**
> **대상**: Claude Code (dev 브랜치 작업)

---

## ⛔ 최우선 원칙: 기존 기능 보호

```
┌──────────────────────────────────────────────────────────────────┐
│  기존 dev 세션 화면(선생님/학생 세션 화면) 절대 수정 금지.          │
│  현재 완벽히 정상 동작 중. 어떤 수정도 하지 않는다.                  │
└──────────────────────────────────────────────────────────────────┘
```

### 🚦 작업 진행 규칙: 한 단계씩 확인 후 진행

```
┌──────────────────────────────────────────────────────────────────┐
│  하나의 작업 단위를 완료할 때마다 반드시 멈추고 사용자 확인을 받는다.  │
│  확인 없이 다음 작업으로 넘어가지 않는다.                             │
└──────────────────────────────────────────────────────────────────┘
```

**작업 단위 (아래 순서대로 하나씩 진행):**

| 순서 | 작업 | 완료 후 확인 사항 |
|---|---|---|
| 1 | LNB 레이아웃 | LNB 4개 메뉴 노출, 기존 세션 화면 정상 동작 |
| 2 | 홈 화면 | 인사 영역 + 카드 + 세션 목록, 더미데이터 표시 |
| 3 | 세션 만들기 모달 | 모달 열림, 필드 입력, 6자리 코드 복사, dev 세션 생성 연결 |
| 4 | 세션 목록 페이지 | 목록 노출, 상태 뱃지, 수정/삭제 더보기 |
| 5 | 내 워크시트 페이지 | PDF 리스트 노출 |
| 6 | 수업 결과 목록 | 종료된 세션 리스트 |
| 7 | 세션 상세 대시보드 | 요약 카드 + 차트 + 학생 테이블 + AI 리포트 |
| 8 | 개별 학생 리포트 | 피드백 다시보기 + 필기 재생 플레이어 UI + 페이지 참여 표 |
| 9 | PDF 다운로드 | 학생별 개별 PDF 생성 |
| 10 | 더미 데이터 정리 | 전체 화면 더미 데이터 통합 점검 |
| 11 | 배포 설정 | Vercel 배포 확인 |

**각 단계 완료 시 반드시:**
1. 작업 내용 요약 보고
2. 기존 기능 회귀 테스트 결과 보고 (세션 생성 → 입장 → 필기 → 동기화 정상 여부)
3. **사용자가 "다음" 또는 "진행"이라고 할 때까지 대기**
4. 확인 없이 절대 다음 단계로 넘어가지 않는다

---

### 🔒 절대 변경 금지 파일 목록

| 파일 | 역할 |
|---|---|
| `Client/src/components/GuestCanvas.tsx` | 게스트 캔버스 (레이아웃, 필기중 상태) |
| `Client/src/components/host-canvas/HostCanvas.tsx` | 호스트 캔버스 (지우기 버튼) |
| `Client/src/components/StudentMiniCanvas.tsx` | 학생 미니 캔버스 (필기중 뱃지 UI) |
| `Client/src/components/StudentDetailView.tsx` | 학생 상세 보기 (A4 크기) |
| `Client/src/components/MonitoringView.tsx` | 모니터링 뷰 (필기중 뱃지 폴링) |
| `Client/src/components/control-bar/SmartpenButton.tsx` | 스마트펜 버튼 (드롭다운) |
| `Client/src/services/pen-protocol.ts` | NeoSmartpen 프로토콜 |
| `Client/src/services/web-bluetooth.service.ts` | Web Bluetooth API |
| `Client/src/services/neosmartpen.service.ts` | 펜 데이터 파싱 |
| `Client/src/services/pen-input.service.ts` | Dot→Stroke 변환 |
| `Client/src/stores/pen-store.ts` | 펜 상태 관리 |

### 🔒 절대 변경 금지 핵심 로직

**localStorage 키 규칙:**
```
nc_strokes_${sessionId}_${userId}      — 완료된 stroke 저장
nc_writing_${sessionId}_${userId}      — 필기중 상태 (timestamp)
nc_participants_${sessionId}           — 참가자 목록
nc_annotations_${sessionId}_${userId}  — 첨삭 데이터
```

**폴링 주기:**
```
호스트 스트로크 폴링: 500ms
참가자 목록 폴링: 1000ms
필기중 상태 폴링: 500ms
첨삭 데이터 폴링: 500ms
```

**Ncode 좌표 변환 (NeoLAB 공식 상수):**
```typescript
NCODE_TO_PT = 2.37066667 × (72 / 25.4) ≈ 6.72
```

**캔버스 크기 (A4 세로 비율):**
```typescript
CANVAS_W = 700
CANVAS_H = Math.round(CANVAS_W * Math.sqrt(2)) // 990
```

### ⚠️ 회귀 방지 체크리스트

새로운 기능 추가 시 반드시 확인:
- [ ] localStorage 키 규칙 준수 (새 키 사용 시 `nc_` 접두사 + 별도 키)
- [ ] 폴링 주기 변경하지 않음
- [ ] Ncode 변환 상수 유지
- [ ] 캔버스 크기 비율 유지
- [ ] 스마트펜 연결 프로토콜 유지
- [ ] 기존 stroke 렌더링 로직 보호
- [ ] 필기중 뱃지 로직 보호

---

## 1. 진입 방식

기존 로그인 화면 유지 (선생님 입장 버튼 → 기존 로그인 로직 사용).

### 흐름

```
[선생님으로 입장] → 기존 로그인 → 호스트 LNB 레이아웃으로 이동
[학생으로 입장]   → 코드 입력(6자리) → 세션 입장 (dev 기존 게스트 로직 연결)
```

---

## 2. LNB 구조 (호스트 전용)

호스트(선생님) 입장 시에만 노출. 메신저 관련 메뉴 없음.

```
🏠 홈
📋 세션 목록
📄 내 워크시트
📊 수업 결과
```

---

## 3. 홈 화면

### 3-1. 상단 인사 영역
- "안녕하세요, {이름}님!"
- "오늘도 스마트한 수업을 시작해볼까요?"

### 3-2. 카드 영역 (2컬럼)

| 좌측: 새 세션 시작 카드 | 우측: 요약 대시보드 |
|---|---|
| (+) 버튼 — 클릭 시 세션 만들기 모달 호출 | 진행 예정: N sessions |
| "즉시 수업을 시작하거나 교재를 업로드하여 세션을 준비하세요" | 평균 활동 시간: N분 |
| | "상세 학습 데이터 리포트 보기 →" (수업 결과 링크) |

### 3-3. 세션 목록 영역
- 제목: "세션 목록" + 우측 "오늘 / 예정된 세션" 필터 탭
- 오늘 기준 최신 5개 노출
- 각 항목: 시간 / 제목 / 강사명 / 첨부 워크시트 / [시작] 버튼 / [...] 더보기
- [...] 더보기 메뉴: **수정, 삭제**
- 더미데이터로 UI 확인 가능하게 구성

---

## 4. 세션 만들기 모달

홈 카드 클릭 / 세션 목록 상단 버튼 → 동일 모달 호출

### 필드 정의

| 필드 | 필수 | 설명 |
|---|---|---|
| 수업 제목 | ✅ | 세션 이름 |
| 날짜/시간 | ❌ | 미입력 시 등록일 기준 |
| 워크시트 업로드 | ❌ | PDF → "내 워크시트" 자동 저장 |
| 참가 예정 인원 수 | ✅ | 참여율(%) 산출 기준 |
| 세션 비밀번호 | 자동생성 | 6자리 코드 자동생성 + **복사 아이콘** |
| 게스트 모드 허용 | 체크박스 | 비회원 참가 여부 |

→ [세션 생성하기] 버튼 클릭 시 dev 세션 생성 로직 연결

### 6자리 코드 확인
- 세션 생성 완료 후 모달에 코드 표시 + **복사 아이콘**
- 세션 목록 카드에도 코드 노출 + **복사 아이콘**

### 참여율 산출
```
참여율(%) = (실제 참가 인원 / 예정 인원) × 100
```

---

## 5. 세션 목록 페이지

- 전체 세션 등록일 최신순
- 상단 [세션 만들기] 버튼 → 동일 모달
- 각 카드: 제목 / 날짜·시간 / 참가자 수(예정/실제) / 상태 뱃지 / [시작] 버튼
- [...] 더보기 메뉴: **수정, 삭제**

### 세션 상태 뱃지

| 상태 | 뱃지 색상 | 전환 조건 |
|---|---|---|
| 예정 | 회색 | 세션 생성 후, 선생님이 아직 입장하지 않은 상태 |
| 진행중 | 파란색 | 선생님이 세션에 입장한 상태 |
| 종료 | 회색(연한) | 세션이 종료된 상태 |

---

## 6. 내 워크시트 페이지

- 세션 만들기에서 업로드한 PDF 리스트만 노출
- 별도 업로드 UI 없음
- 다운로드 옵션: NCode PDF / .np2
- 삭제 기능 없음

---

## 7. 수업 결과 페이지

> **철학**: 평가/채점/점수화 지양. 종이 기반 수업을 디지털로 "기록/복습"하는 도구.

### 7-1. 하지 않는 것 (❌)
- 종합 점수, 성취도 점수
- 오답률/채점 기반 지표
- 문항 단위 분석
- "지도 필요", "부진" 등 평가형 문장
- 회차별 활동 추이

### 7-2. 하는 것 (✅)
1. 수업 활동 기록 (활동 시간 / 참여 페이지)
2. 교사가 남긴 피드백(첨삭)의 재생/요약
3. 페이지 단위 러프 통계
4. 복습을 쉽게 만드는 구간 점프 내비게이션
5. AI 리포트: 페이지별 활동 시간이 0인 학생 표시 (수업 종료 후 사후 분석)

### 7-3. 사용자 흐름

```
수업 종료 → 세션 결과 자동 저장
→ 수업 결과 목록에서 세션 확인
→ 세션 클릭 → 세션 상세 대시보드
→ 학생 클릭 → 개별 학생 리포트
→ PDF 다운로드
```

### 7-4. 수업 결과 목록

| 세션명 | 날짜·시간 | 진행 시간(분) | 참여 인원 |
|---|---|---|---|

### 7-5. 세션 상세 대시보드

**상단 요약 카드:**
- 진행 시간
- 참여 학생 수
- 평균 활동 시간
- 평균 참여 페이지 수

**차트:**
- 학생별 활동 시간 막대 그래프 + 참여 페이지 수

**학생 테이블:**

| 학생명 | 활동 시간 | 참여 페이지 수 | 피드백(첨삭) 수 | 상세 | PDF |
|---|---|---|---|---|---|

- PDF 컬럼: 각 학생 행에 **개별 다운로드 버튼**

**AI 리포트 영역:**
- "⚠️ 활동이 감지되지 않은 학생" 섹션
- 페이지별 활동 시간이 0인 학생 목록 자동 표시
- 수업 중 방해 없이, 종료 후 사후 확인용
- 더미데이터로 구성

### 7-6. 개별 학생 리포트

**상단 요약:**
- 세션명 · 날짜
- 활동 시간
- 참여 페이지 수
- 피드백 수

**선생님 피드백(첨삭) 다시보기:**

| 페이지 번호 | 시간 구간 | 음성 요약 (1~3줄) | 구간 재생 버튼 |
|---|---|---|---|

- 음성 요약: 선생님이 수업/첨삭 시 음성으로 말한 내용의 텍스트 요약
- 신규 기능 아이디어 — 기능 구현 로직 없음. **더미 데이터로 구성**

**필기 재생 플레이어:**

```
┌─────────────────────────────────────────────────────┐
│ 펜 캡처 원본           [+][-][🖼 이미지 보기]  1/2  │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │                                             │    │
│  │         필기 캔버스 영역                      │    │
│  │    학생 필기(검정) + 선생님 첨삭(빨간색)       │    │
│  │         색상으로 구분                         │    │
│  │                                             │    │
│  └─────────────────────────────────────────────┘    │
│  [◀]                                          [▶]   │
│                                                     │
│  [▶] ━━━━━━━●━━━━━━━━  12:12 / 11:45  [1x] [⛶]   │
└─────────────────────────────────────────────────────┘
```

| 영역 | 설명 |
|---|---|
| 상단 | "펜 캡처 원본" 타이틀 + 페이지 네비게이션(1/2, 좌우 화살표) + 줌 컨트롤(+/-) + 이미지 보기 토글 |
| 중앙 | 필기 캔버스 — 학생 필기(검정) + 선생님 첨삭(빨간색) 색상 구분 |
| 하단 바 | ▶ 재생/일시정지 + 프로그레스바(시크 가능) + 현재시간/전체시간 + 속도 토글(1x/2x) + 전체화면 버튼 |

**필기 재생 토글:**
- 전체 필기 / 교사만 / 교사+학생 토글
- 속도: 1x / 2x
- 피드백 카드 클릭 시 → 해당 구간으로 점프
- 신규 기능 아이디어 — **UI만 더미로 구성**

**페이지 참여 요약 표:**

| 페이지 | 참여 여부 | 활동 시간 |
|---|---|---|

**PDF 다운로드 (jsPDF + html2canvas):**
- 기본 정보 / 활동 요약 / 피드백 리스트 / 페이지 참여 요약 표
- 학생별 개별 다운로드

---

## 8. 반드시 살릴 dev 핵심 기능

```
⛔⛔⛔ 절대 수정 금지 ⛔⛔⛔
```

### 보존 흐름

```
세션 생성 → 6자리 코드 발급 → 호스트 세션 입장 화면
                                  ↕ 실시간 연결 (localStorage 폴링)
학생 코드 입력 → 게스트 세션 입장 화면
```

### 보존 기능 목록
1. 마우스 필기 (스트로크)
2. 호스트/게스트 간 실시간 입장 확인
3. 스트로크 공유
4. 첨삭 모드
5. 스마트펜 Web Bluetooth 연결
6. 스마트펜 Dot → Stroke 변환 및 실시간 동기화
7. 호스트 캔버스 지우기
8. 학생 필기중 뱃지 (✏️ 필기중 초록 / 첨삭중 빨강)

---

## 9. 배포

### 프로젝트 구조
- **프론트엔드 only**: React 19 + TypeScript + Vite
- **실시간 동기화**: localStorage 폴링 (서버 불필요)
- **WebSocket/백엔드 서버 없음**

### 추천: Vercel

```bash
# 1. Vercel CLI 설치
npm i -g vercel

# 2. 프로젝트 루트에서 배포
cd Client
vercel

# 3. 이후 push할 때마다 자동 배포
# GitHub 연결 시 main/dev 브랜치별 자동 배포
```

**Vercel 선택 이유:**
- 백엔드 없이 정적 빌드만 배포하면 되므로 가장 적합
- GitHub 연결 → push마다 자동 빌드/배포
- HTTPS 자동 적용
- 무료 플랜으로 충분
- `vercel.app` 도메인 즉시 발급 (커스텀 도메인도 가능)

**대안:**
- Netlify: Vercel과 거의 동일. 드래그앤드롭 배포도 가능
- GitHub Pages: 완전 무료, 다만 SPA 라우팅 설정 필요

### 환경별 배포 전략

| 브랜치 | 배포 URL | 용도 |
|---|---|---|
| dev | preview URL (자동 생성) | 개발/테스트 |
| main | production URL | 실 사용 |

---

## 10. 더미 데이터 구성 가이드

실제 데이터 연결 전까지 모든 UI는 더미 데이터로 확인 가능하게 구성한다.

| 화면 | 더미 데이터 항목 |
|---|---|
| 홈 요약 대시보드 | 진행 예정 N개, 평균 활동 시간 N분 |
| 세션 목록 | 5~10개 세션 (예정/진행중/종료 혼합) |
| 내 워크시트 | 3~5개 PDF 항목 |
| 수업 결과 목록 | 3~5개 종료된 세션 |
| 세션 상세 | 학생 5명, 활동 시간/페이지 수/피드백 수 |
| AI 리포트 | 활동 시간 0인 학생 1~2명 |
| 개별 학생 리포트 | 피드백 3개, 음성 요약 텍스트, 페이지별 참여 여부 |
| 필기 재생 | UI만 구성 (재생/시크/속도 컨트롤 동작) |

---

## 기술 스택

| 항목 | 기술 |
|---|---|
| Frontend | React 19, TypeScript, Vite, Zustand |
| 실시간 동기화 | localStorage 폴링 (0.5s~1s) |
| 스마트펜 | Web Bluetooth API, NeoSmartpen Protocol V2 |
| 좌표 변환 | Ncode → 72 DPI Point |
| 캔버스 | HTML5 Canvas, A4 세로 비율 (700×990) |
| PDF 생성 | jsPDF + html2canvas |
| 배포 | Vercel |

---

## 구현 체크리스트

- [ ] LNB 레이아웃 (4개 메뉴)
- [ ] 홈 화면 (인사 + 카드 + 세션 목록)
- [ ] 세션 만들기 모달 + 6자리 코드 복사
- [ ] 세션 목록 페이지 + 상태 뱃지 + 수정/삭제
- [ ] 내 워크시트 페이지
- [ ] 수업 결과 목록
- [ ] 세션 상세 대시보드 + 차트 + AI 리포트
- [ ] 개별 학생 리포트
- [ ] 피드백 다시보기 (음성 요약 더미)
- [ ] 필기 재생 플레이어 UI
- [ ] PDF 다운로드 (학생별 개별)
- [ ] 더미 데이터 전체 구성
- [ ] Vercel 배포 설정
- [ ] ⚠️ **dev 세션 화면 미수정 확인 (최우선)**
